public Integer nextPrime(Integer num) {
    //code here
    Integer nextprime;
    for (Integer i=num;i<num+100;i++){
        for(Integer j=2;j<i;j++){
            if(Math.mod(i,j)==0){
                nextprime=i;
                break;
            }
        }
    }
    return nextprime;
}